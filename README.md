# LAB 02

## The `html`/`css` one

### TODO
- Fork and clone the repository
- Open repository in VSCode and enable the Live Server extension
- Replicate `imgs/portfolio-landing-page.png` by re-structuring `01-exercise/portfolio.html`
  - All elements are there, they're just not in the right structure; they've been *unnested*
  - Element types (`<section>`, `<nav>`, etc) and class names can help re-create the structure
  - All `css` specifications are used at least once
- Alternatively, replicate `imgs/studio-landing-page.png` by re-structuring `01-exercise/studio.html`
